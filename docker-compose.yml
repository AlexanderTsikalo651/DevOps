version: '3'
services:
  backend:
    build: ./backend1  # Ensure this path is correct
    ports:
      - "5001:5000"
    environment:
      - FLASK_APP=back
    command: ["flask", "run", "--host=0.0.0.0"]
  frontend:
    build: C:\\Users\\tsika\\PycharmProjects\\DevOps\\frontend1
    ports:
      - "5002:5000"
    environment:
      - FLASK_APP=add
    command: ["flask", "run", "--host=0.0.0.0"]
