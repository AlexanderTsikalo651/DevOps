version: '3.9'

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    image: frontend:2.0
    depends_on:
      - backend
    networks:
      - devops-network
    volumes:
      - ./data:/app/data
    restart: always

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: backend:2.0
    networks:
      - devops-network
    volumes:
      - ./data:/app/data
    restart: always

networks:
  devops-network:
    driver: bridge
