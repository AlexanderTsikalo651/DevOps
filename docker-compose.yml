services:
  frontend1:
    build: .
    ports:
      - "5002:5002"
    depends_on:
      - nginx
    environment:
      - BACKEND_URL=http://localhost:5000

  backend1:
    build: .
    ports:
      - "5000:5000"

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
